import { createCrudRouter } from '../utils/createCrudRouter.js'; import { ROLES } from '../middleware/auth.js';
export default createCrudRouter({ table:'opportunities', label:'opportunity', roles:ROLES.OPPORTUNITIES, fields:['slug','title','description','benefits','responsibilities','eligibility','badge','application_url','application_status','deadline','is_published','is_featured','sort_order'], required:['title','description'], jsonFields:['benefits','responsibilities'], boolFields:['is_published','is_featured'], urlFields:['application_url'], slugField:'slug', defaultSort:'sort_order ASC, created_at DESC' });
